# WonGames API

<p style="text-align: center;">
  <a href="https://www.pedroestevao.com">
    <img src="https://res.cloudinary.com/dge3g9rcw/image/upload/v1741210790/github/althy58rnu8ahhjdl3kv.png" alt="illustrative image" />
  </a>
</p>

The WonGames API is responsible for content management, users, authentication, wishlists, orders, payments, media, email delivery, and database persistence.

It is built with Strapi 5, TypeScript, PostgreSQL, and GraphQL.

For repository-wide setup and workspace commands, see the [root documentation](../README.md).

## Responsibilities

The API is responsible for:

- Game catalog management
- Category and platform management
- Developer and publisher management
- Home page content
- Banner management
- Recommendations
- User registration and authentication
- Password recovery
- Wishlist persistence
- Order creation and retrieval
- Stripe payment integration
- Cloudinary media storage
- SMTP email delivery
- REST and GraphQL endpoints
- Strapi administration panel
- Database migrations
- Authorization and permission enforcement

## Technology Stack

- Node.js 20
- Bun 1.3+
- TypeScript 5
- Strapi 5.10
- PostgreSQL
- GraphQL
- Strapi Users & Permissions
- Stripe
- Cloudinary
- Nodemailer
- CKEditor

## Directory Structure

```text
api/
├── config/
│   ├── admin.ts
│   ├── api.ts
│   ├── database.ts
│   ├── middlewares.ts
│   ├── plugins.ts
│   └── server.ts
├── database/
│   └── migrations/
├── public/
│   └── uploads/
├── src/
│   ├── api/
│   ├── extensions/
│   ├── utils/
│   └── index.ts
├── types/
│   └── generated/
├── .env.example
├── package.json
├── tsconfig.json
└── README.md
```

## Main Directories

### `config`

Contains application configuration for:

- HTTP server
- Database
- Administration panel
- REST API
- Middlewares
- Plugins
- Email provider
- Upload provider
- GraphQL

### `database/migrations`

Contains versioned database migrations.

### `src/api`

Contains content types, controllers, routes, and services for the project domains.

### `src/extensions`

Contains customizations for Strapi plugins, including user-related extensions.

### `src/utils`

Contains reusable application utilities.

### `types/generated`

Contains TypeScript definitions generated by Strapi.

Generated files must not be edited manually.

## Domains

The main API domains include:

| Domain        | Responsibility                        |
| ------------- | ------------------------------------- |
| `banner`      | Promotional and informational banners |
| `category`    | Game categories                       |
| `developer`   | Game development studios              |
| `game`        | Main game catalog data                |
| `home`        | Home page content                     |
| `order`       | Orders and payment operations         |
| `platform`    | Supported game platforms              |
| `publisher`   | Game publishers                       |
| `recommended` | Recommended content                   |
| `wishlist`    | User wishlist data                    |
| `user`        | Users and authentication extensions   |

Each domain follows the standard Strapi structure:

```text
src/api/<domain>/
├── content-types/
├── controllers/
├── routes/
└── services/
```

Custom routes should remain separate from automatically generated Strapi routes.

## Requirements

- Node.js 20
- Bun 1.3 or later
- PostgreSQL
- Stripe credentials for payment operations
- Cloudinary credentials for media uploads
- SMTP credentials for transactional emails

Dependencies are installed from the repository root:

```bash
bun install
```

Do not create a separate `api/bun.lock`.

The repository must maintain only:

```text
../bun.lock
```

## Environment Variables

Create the local environment file:

```bash
cp .env.example .env
```

Example configuration:

```env
HOST=0.0.0.0
PORT=1337
APP_URL=http://localhost:1337

APP_KEYS=key1,key2,key3,key4
API_TOKEN_SALT=
ADMIN_JWT_SECRET=
TRANSFER_TOKEN_SALT=
JWT_SECRET=

DATABASE_CLIENT=postgres
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432
DATABASE_NAME=wongames
DATABASE_USERNAME=wongames
DATABASE_PASSWORD=wongames
DATABASE_SCHEMA=public
DATABASE_SSL=false

SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=wongames

STRIPE_SECRET_KEY=

GOG_URL=
GOG_API_URL=
```

## Environment Reference

### Server

| Variable  | Required | Description                          |
| --------- | -------: | ------------------------------------ |
| `HOST`    |      Yes | Network interface used by the server |
| `PORT`    |      Yes | API HTTP port                        |
| `APP_URL` |      Yes | Public API URL                       |

### Strapi Secrets

| Variable              | Required | Description                                |
| --------------------- | -------: | ------------------------------------------ |
| `APP_KEYS`            |      Yes | Internal Strapi application keys           |
| `API_TOKEN_SALT`      |      Yes | Salt used for API tokens                   |
| `ADMIN_JWT_SECRET`    |      Yes | JWT secret for the administration panel    |
| `TRANSFER_TOKEN_SALT` |      Yes | Salt used for transfer tokens              |
| `JWT_SECRET`          |      Yes | Secret used for user authentication tokens |

### Database

| Variable            | Required | Description                                                             |
| ------------------- | -------: | ----------------------------------------------------------------------- |
| `DATABASE_CLIENT`   |      Yes | Database driver, normally `postgres`                                    |
| `DATABASE_HOST`     |      Yes | PostgreSQL host                                                         |
| `DATABASE_PORT`     |      Yes | PostgreSQL port                                                         |
| `DATABASE_NAME`     |      Yes | Database name                                                           |
| `DATABASE_USERNAME` |      Yes | Database user                                                           |
| `DATABASE_PASSWORD` |      Yes | Database password                                                       |
| `DATABASE_SCHEMA`   |       No | PostgreSQL schema                                                       |
| `DATABASE_SSL`      |       No | Enables SSL connections                                                 |
| `DATABASE_URL`      |       No | Complete connection string when supported by the deployment environment |

### SMTP

| Variable        |           Required | Description      |
| --------------- | -----------------: | ---------------- |
| `SMTP_HOST`     | Required for email | SMTP server host |
| `SMTP_PORT`     | Required for email | SMTP server port |
| `SMTP_USERNAME` | Required for email | SMTP username    |
| `SMTP_PASSWORD` | Required for email | SMTP password    |

### Cloudinary

| Variable                |             Required | Description                            |
| ----------------------- | -------------------: | -------------------------------------- |
| `CLOUDINARY_CLOUD_NAME` | Required for uploads | Cloudinary account name                |
| `CLOUDINARY_API_KEY`    | Required for uploads | Cloudinary API key                     |
| `CLOUDINARY_API_SECRET` | Required for uploads | Cloudinary API secret                  |
| `CLOUDINARY_FOLDER`     |                   No | Folder used to organize project assets |

### Stripe

| Variable            |              Required | Description            |
| ------------------- | --------------------: | ---------------------- |
| `STRIPE_SECRET_KEY` | Required for payments | Private Stripe API key |

### External Catalog Integration

| Variable      |                     Required | Description          |
| ------------- | ---------------------------: | -------------------- |
| `GOG_URL`     | Depends on integration usage | Main GOG service URL |
| `GOG_API_URL` | Depends on integration usage | GOG API URL          |

## Generating Secrets

Use strong, random values that differ between development, staging, and production.

Generate a secret:

```bash
openssl rand -base64 32
```

`APP_KEYS` requires multiple comma-separated values:

```env
APP_KEYS=first-key,second-key,third-key,fourth-key
```

Never reuse production secrets in local development or CI.

## Database

PostgreSQL is the primary database.

The database configuration is located in:

```text
config/database.ts
```

When the API runs directly on the host machine and PostgreSQL is exposed locally:

```env
DATABASE_HOST=127.0.0.1
```

When the API and PostgreSQL run inside the same Docker Compose network:

```env
DATABASE_HOST=postgres
```

The database user must have permission to:

- Connect to the configured database
- Create and alter tables
- Create indexes
- Execute migrations
- Read and write application data

## Running the API

From the repository root:

```bash
bun run dev:api
```

Alternatively:

```bash
bun run --cwd ./api dev
```

The API will be available at:

```text
http://localhost:1337
```

The administration panel will be available at:

```text
http://localhost:1337/admin
```

On the first run, Strapi will request the creation of an administrator account.

## Available Scripts

The following scripts are declared in `api/package.json`:

| Command                      | Description                                              |
| ---------------------------- | -------------------------------------------------------- |
| `bun run --cwd ./api dev`    | Starts Strapi in development mode                        |
| `bun run --cwd ./api start`  | Starts Strapi in production mode                         |
| `bun run --cwd ./api build`  | Builds the administration panel and production artifacts |
| `bun run --cwd ./api strapi` | Runs the Strapi CLI                                      |

Root aliases are also available:

```bash
bun run dev:api
bun run build:api
```

Examples using the Strapi CLI:

```bash
bun run --cwd ./api strapi -- --help
```

```bash
bun run --cwd ./api strapi -- content-types:list
```

The separator `--` forwards the remaining arguments to the Strapi CLI.

## Available Interfaces

### Administration Panel

```text
http://localhost:1337/admin
```

The administration panel is used to:

- Manage content types
- Create and edit games
- Manage categories and platforms
- Manage developers and publishers
- Upload and organize media
- Review users
- Configure roles and permissions
- Manage home page content

### GraphQL

```text
http://localhost:1337/graphql
```

The GraphQL endpoint is primarily consumed by the client.

### REST API

```text
http://localhost:1337/api
```

Available endpoints depend on the registered content types, custom routes, and configured permissions.

## Permissions

Strapi permissions are separated by role.

The main access contexts are:

- Public
- Authenticated
- Administrative roles

After creating or changing a content type:

1. Review public permissions.
2. Enable only the required operations.
3. Review authenticated permissions.
4. Verify user ownership restrictions.
5. Test REST endpoints.
6. Test GraphQL queries and mutations.

An endpoint must not be considered protected only because the client does not expose it.

Authorization must be enforced by the API.

## GraphQL

The GraphQL plugin exposes queries and mutations based on Strapi content types.

After modifying a content type:

1. Restart the API.
2. Verify the GraphQL endpoint.
3. Regenerate the client GraphQL artifacts.
4. Run the client type validation.
5. Run the client tests.
6. Build both applications.

From the repository root:

```bash
bun run codegen
bun run typecheck:client
bun run --cwd ./client test:ci
bun run build
```

Breaking GraphQL changes should preferably update the API and client within the same pull request.

## Authentication

Authentication uses the Strapi Users & Permissions plugin.

The flow includes:

- User registration
- User authentication
- JWT generation
- Password recovery
- Password reset
- User profile retrieval
- User profile updates
- Association of orders and wishlists with authenticated users

User content type extensions are located under:

```text
src/extensions/
```

The API must validate authentication and ownership before exposing or modifying private records.

## Orders and Payments

The `order` domain is responsible for the order and payment flow.

The expected payment flow is:

1. The API receives product identifiers from the client.
2. The API retrieves the current products and prices.
3. The API calculates the authoritative order total.
4. The API creates a Stripe PaymentIntent.
5. The client confirms the payment through Stripe.js.
6. The API verifies the payment result.
7. The order is persisted and associated with the authenticated user.

The amount sent by the client must never be considered authoritative.

The API must validate:

- User authentication
- Product existence
- Current prices
- Currency
- Payment status
- Duplicate requests
- Order ownership
- Stripe errors

The Stripe secret key must exist only in the API environment:

```env
STRIPE_SECRET_KEY=
```

## Media Uploads

Cloudinary is used as the external upload provider.

Configuration is provided through:

```env
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=wongames
```

The local `public/uploads` directory should only be treated as development storage or controlled local content when the Cloudinary provider is not being used.

Upload configuration should consider:

- Allowed file types
- Maximum upload size
- Environment-specific folders
- Access permissions
- Deletion behavior
- Credential security

Cloudinary credentials must not be exposed to the client.

## Email Delivery

Nodemailer is used through the Strapi email provider.

The provider configuration is located in:

```text
config/plugins.ts
```

Email-dependent features include password recovery and other transactional messages.

Production email configuration should:

- Use a verified domain
- Use TLS when available
- Configure SPF
- Configure DKIM
- Configure DMARC
- Define the sender through environment variables
- Avoid hardcoded personal addresses
- Avoid logging message contents containing sensitive information

## Database Migrations

Migrations are stored in:

```text
database/migrations/
```

Each migration should:

- Have a single responsibility
- Be safe for existing data
- Consider previously populated databases
- Avoid destructive operations without a transition strategy
- Be tested before production deployment
- Be committed with the related application change

Direct production database modifications should not replace versioned migrations.

## Content Types

Content type schemas are located at:

```text
src/api/<domain>/content-types/<content-type>/schema.json
```

When creating or changing a content type:

- Use consistent naming.
- Define required fields explicitly.
- Review relationships and cardinality.
- Avoid duplicated data.
- Review draft and publish behavior.
- Regenerate related types.
- Validate the GraphQL contract.
- Update the client when necessary.
- Review public and authenticated permissions.

## Controllers, Services, and Routes

### Controllers

Controllers should:

- Validate request data
- Validate authentication and authorization
- Delegate business rules to services
- Return consistent responses
- Use appropriate HTTP status codes
- Avoid exposing internal errors

### Services

Services should contain:

- Business rules
- Reusable domain logic
- Payment operations
- External service integration
- Database orchestration

Reusable business logic should not remain inside controllers.

### Routes

Custom routes should explicitly define:

- HTTP method
- Path
- Handler
- Policies
- Middlewares
- Authentication requirements

## Error Handling

Errors should:

- Use appropriate HTTP status codes
- Return safe and actionable messages
- Avoid production stack traces
- Avoid exposing tokens or credentials
- Preserve diagnostic context in internal logs
- Handle expected business failures explicitly
- Distinguish validation, authorization, integration, and infrastructure errors

External integration errors should be translated into stable application responses instead of exposing provider-specific details directly to the client.

## Production Build

Build the API from the repository root:

```bash
bun run build:api
```

Start the production application:

```bash
bun run --cwd ./api start
```

Production must not use:

```bash
strapi develop
```

Before deployment, verify:

- PostgreSQL connectivity
- Strapi application secrets
- Cloudinary configuration
- SMTP configuration
- Stripe secret key
- Public API URL
- Content type permissions
- Administration panel build
- CORS configuration

## Deployment Requirements

The API production environment should provide:

- Node.js 20
- A persistent application process
- PostgreSQL
- HTTPS
- External media storage
- Secure environment variables
- Centralized logs
- Database backups
- Availability monitoring

The database must not depend on an ephemeral application filesystem.

## Backup Strategy

A production backup strategy should include:

- Scheduled PostgreSQL backups
- Retention policy
- Encryption
- Storage outside the main application server
- Periodic restoration tests
- Version tracking between application releases and backups

Cloudinary assets should follow the retention and backup capabilities of the configured account.

## Continuous Integration

The API workflow is located at:

```text
.github/workflows/api-ci.yml
```

Because the project uses a root Bun lockfile, dependencies must be installed from the repository root:

```bash
bun install --frozen-lockfile
```

The build command should use the root alias or `--cwd`:

```bash
bun run build:api
```

The workflow should monitor changes to:

```text
api/**
package.json
bun.lock
.github/workflows/api-ci.yml
```

The workflow may also provide PostgreSQL as a service when integration tests, application bootstrap validation, or database-dependent checks are added.

CI environment variables must use non-production values.

## Validation Before Commit

Build the API:

```bash
bun run build:api
```

Changes affecting the GraphQL contract should also validate the client:

```bash
bun run codegen
bun run typecheck:client
bun run --cwd ./client test:ci
bun run build:client
```

The recommended next quality improvement for the API is automated coverage for:

- Custom controllers
- Order services
- Price calculation
- Wishlist ownership
- Order authorization
- Stripe integration behavior
- External service failures

## Troubleshooting

### Bun cannot find the API scripts

Verify the root `package.json` workspaces and scripts:

```json
{
	"workspaces": ["client", "api"]
}
```

Verify the API package name:

```json
{
	"name": "@wongames/api"
}
```

Then run from the repository root:

```bash
bun run --cwd ./api --help
```

### API cannot connect to PostgreSQL

Verify:

- PostgreSQL is running.
- `DATABASE_HOST` is correct.
- `DATABASE_PORT` is correct.
- The database exists.
- Username and password are correct.
- SSL configuration matches the environment.
- The database is reachable from the API process.

### GraphQL is unavailable

Verify:

- The GraphQL plugin is installed.
- Strapi started successfully.
- The plugin configuration is valid.
- The requested content type is registered.
- The relevant role has permission.
- No schema generation errors appear in the logs.

### Media uploads fail

Verify:

- Cloudinary credentials.
- Environment variable names.
- Account limits.
- File size.
- File type.
- Upload provider configuration.

### Emails are not delivered

Verify:

- SMTP host.
- SMTP port.
- Username and password.
- TLS requirements.
- Sender configuration.
- Provider restrictions.
- Application logs.

### The administration panel build fails

Remove local build artifacts and try again:

```bash
rm -rf api/.cache api/build api/dist
bun run build:api
```

## Security

- Restrict access to the administration panel.
- Use strong administrator passwords.
- Review public and authenticated permissions.
- Restrict CORS to required origins.
- Never log passwords or JWTs.
- Keep secrets in environment variables.
- Use HTTPS in production.
- Rotate secrets periodically.
- Validate external service responses.
- Calculate prices and totals on the server.
- Add rate limiting to sensitive endpoints.
- Keep Strapi and plugins updated.
- Review third-party plugins before major upgrades.

---

**Built with ☕ by [Pedro Estevão](https://www.pedroestevao.com)**
